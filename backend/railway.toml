# Railway deploy — set Root Directory ke: backend
# Build: npm run build | Start: npm start

[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
